# Tempo Timesheet Automation - Python Dependencies
# Install with: pip install -r requirements.txt

# HTTP requests
requests>=2.31.0

# Holiday detection by country/state (US, India, 100+ countries)
holidays>=0.40

# Windows toast notifications (Action Center) -- Mac uses osascript instead
winotify>=1.1.0; sys_platform == 'win32'

# System tray app
pystray>=0.19.0
Pillow>=10.0.0

# Note: All other dependencies (json, logging, smtplib, etc.)
# are part of Python's standard library
